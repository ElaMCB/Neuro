name: Test Neuro

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Test Python files
      run: |
        cd src/neuro
        python -c "import parser; import compiler; print('SUCCESS: All imports work')"
